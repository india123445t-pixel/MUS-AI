# AQLEVON P4 A0 public SFT Candidate path — 2026-09-25

This branch is an explicit NEW scientific arm: `P4_A0_SFT_LORA_CONTROL`. It starts from the A1 Worker03 source at `83db1c03f176539784aa39a4474982cdcde3c42c` but does not modify the frozen A1 training command or PR #30. The A1 Recovery20A attempt reached global steps 1–12 yet produced an empty 16-byte saved adapter and zero reward/advantage/gradient at all steps; it is not a Candidate.

A0 follows the existing Worker01 P4 plan: Qwen3.5-4B-Base `daa9c16f371249f9ad1c75a9ed6f956c08ea08f5`, BF16, PEFT LoRA r=4 alpha=4 dropout=0 q_proj/v_proj only on eight full-attention layers (16 modules, 32 A/B tensors). Training uses only Worker02-admitted public reference answers from shard `59480e9ff48b36a0efb77a36d3e35d9f656ef4dee0ce18489d3017c92b2a0d49` and manifest `f7499362fdd7e6fd4bc91682a5f1c03767c98685c045ad50a712711c6c4ad55f`, seed1701, 12 optimizer updates, exactly 4 unique examples per update / 48 total, next-token cross-entropy on the answer only, AdamW LR 1e-5 to floor 1e-6, cosine with 10% warmup and weight decay 0.10. No teacher or sealed/private evaluation data.

Implementation: `p4_a0_sft_candidate.py` checks public data hashes and topology, rejects zero gradients, saves 32 trained LoRA tensors, verifies nonzero B elements, reloads through PEFT, compares tensor-state hashes, and writes a self-hashed Candidate Manifest, receipt, and Worker05 handoff index. `p4_a0_runpod_launch.sh` stages only pinned public inputs and the exact base model, samples resources, then runs this program under a bounded timer. Any Candidate still requires independent Worker05 evaluation and Manager acceptance before a capability-gain claim. No 27B, G1 rerun, main merge, or PR #30 merge is authorized.
